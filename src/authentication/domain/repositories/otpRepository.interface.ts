// Re-export from new ports location for backward compatibility
// TODO: Update all imports to use @auth/domain/ports/repositories directly
export type { IOtpRepository } from '@auth/domain/ports/repositories';
