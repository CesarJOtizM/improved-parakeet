// Re-export from new ports location for backward compatibility
// TODO: Update all imports to use @movement/domain/ports/repositories directly
export type { IMovementRepository } from '@movement/domain/ports/repositories';
