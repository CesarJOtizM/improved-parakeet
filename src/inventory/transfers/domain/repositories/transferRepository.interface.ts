// Re-export from new ports location for backward compatibility
// TODO: Update all imports to use @transfer/domain/ports/repositories directly
export type { ITransferRepository } from '@transfer/domain/ports/repositories';
