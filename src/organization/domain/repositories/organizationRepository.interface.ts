// Re-export from new ports location for backward compatibility
// TODO: Update all imports to use @organization/domain/ports/repositories directly
export type { IOrganizationRepository } from '@organization/domain/ports/repositories';
