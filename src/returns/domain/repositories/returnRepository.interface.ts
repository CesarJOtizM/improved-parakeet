// Re-export from new ports location for backward compatibility
// TODO: Update all imports to use @returns/domain/ports/repositories directly
export type { IReturnRepository } from '@returns/domain/ports/repositories';
